export type Blog = {
    Title: string
    Text: string
}

export type Contact = {
    Subject: string
    Email: string
    Text: string
}

export type Work = {
    Title: string
    Text: string
}

export type User = {
    UserName: string
    PassWord: string
}

export type Token = {
    AccessToken: string
    RefreshToken: string
}

export type Tag = {
    Name: string
}